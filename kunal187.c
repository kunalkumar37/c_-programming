#include<stdio.h>
#include<string.h>
main()
{
struct student
{
char str[6];
unsigned int age;
char address[20];
};
struct student s1= {"kunal",20,"bihar"};
printf("%d",students1.age);
printf("%d",sizeof(struct student));
return 0;
}
