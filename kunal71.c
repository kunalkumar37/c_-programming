#include<stdio.h>
main(void)
{
    int row ,col,i,j;
    printf("Enter the number of row :\n");
    scanf("%d",&row);
    printf("Enter the number of column :\n");
    scanf("%d",&col);
    for(i=1;i<=row;i++)
    {
        for(j=1;j<=col;j++)
        {
            if(2*row-)
        }
    }
}